# exchange


### database structure

```sql

CREATE TABLE links (id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL, username TEXT NOT NULL UNIQUE, email TEXT NOT NULL, pwd_hash TEXT NOT NULL, joined INTEGER);

CREATE TABLE items (id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL, name TEXT NOT NULL, desc TEXT NOT NULL, user_id INTEGER NOT NULL, img_url TEXT NOT NULL, uploaded INTEGER);

```
